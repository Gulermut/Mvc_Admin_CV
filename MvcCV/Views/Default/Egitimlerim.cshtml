@using MvcCV.Models.Entity
@model List<TBLEGİTİM>

<div class="resume-section-content">
    <h2 class="mb-5">Egitim Hayatım</h2>
    @foreach (var item in Model) { 
    <div class="d-flex flex-column flex-md-row justify-content-between mb-5">
        <div class="flex-grow-1">
            <h3 class="mb-0">@item.BASLIK</h3>
            <div class="subheading mb-3">@item.ALTBASLIK1</div>
            <div>@item.ALTBASLIK2</div>
            <p>GNOA: @item.GNO</p>
        </div>
        <div class="flex-shrink-0"><span class="text-primary">@item.TARİH</span></div>
    </div>
    }
    </div>

